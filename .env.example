# Variáveis de ambiente para configurar no Railway Dashboard
# NÃO commitar esse arquivo com valores reais

# Chave SSH do bastion em base64
# Para gerar: cat ~/.ssh/id_rsa_boobam | base64 | tr -d '\n'
SSH_PRIVATE_KEY=

# Banco de dados (via tunnel local na porta 3307)
MYSQL_HOST=127.0.0.1
MYSQL_PORT=3307
MYSQL_USER=vinicius
MYSQL_PASS=P1FIwelat2f8
MYSQL_DB=boobam

# Modo de transporte SSE (obrigatório para claude.ai)
TRANSPORT_TYPE=sse
PORT=3000
